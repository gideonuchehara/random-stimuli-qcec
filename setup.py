"""Setup script for the Random Stimuli QCEC package."""

from setuptools import setup, find_packages

setup(
    name="random_stimuli_qcec",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    # Add more package metadata if needed
)